# Sherlock-Holmes

Python code hosted on railway. Creates API endpoint that can be passed .mp3 recordings. Like Sherlock Holmes, it analyzes and solves the mystery... of which notes comprise the audio.

TO RUN:
source venv/bin/activate
uvicorn main:app --reload
